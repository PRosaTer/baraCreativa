# DIRECTORIOS DE MÓDULOS DE NODE (CRÍTICO)
node_modules/              # Si hay un node_modules directo en la raíz (ej. para un lerna/nx mono-repo)
front/node_modules/        # node_modules específico del frontend
backend/node_modules/      # node_modules específico del backend

# ARCHIVOS DE BUILD (CRÍTICO)
front/build/               # Salida de build del frontend (ej. Create React App)
front/dist/                # Otra posible salida de build del frontend
front/out/                 # Otra posible salida de build del frontend
front/.next/               # Para proyectos Next.js (aparece en tu status, CRÍTICO IGNORAR)
front/.svelte-kit/         # Para proyectos SvelteKit
backend/dist/              # Salida de build del backend (NestJS)
backend/coverage/          # Reportes de cobertura de tests del backend

# ARCHIVOS ESPECÍFICOS DE TYPESCRIPT / NEXT.JS
front/next-env.d.ts        # Este archivo es generado por Next.js, generalmente se ignora
*.tsbuildinfo              # Archivos de información de build de TypeScript

# ARCHIVOS DE LOGS
*.log
npm-debug.log*
yarn-debug.log*
lerna-debug.log*

# VARIABLES DE ENTORNO
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
*.env # Si usas archivos .env en otras subcarpetas

# ARCHIVOS ESPECÍFICOS DEL EDITOR / OS
.vscode/                   # Configuración de VS Code (opcional, pero común ignorar)
.idea/                     # Configuración de IntelliJ/WebStorm
*.sublime-project
*.sublime-workspace
.DS_Store                  # Archivos de macOS
Thumbs.db                  # Archivos de Windows

# OTROS ARCHIVOS TEMPORALES
.eslintcache
.turbo/                    # Si usas Turborepo